# Email Configuration
# SMTP Settings (Example: Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Admin Email
ADMIN_EMAIL=admin@rathodlaw.com

# Testing Credentials (For Development Only)
# You can use these testing credentials for development:
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=testing.account@ethereal.email
# SMTP_PASS=testing-password
# ADMIN_EMAIL=test-admin@example.com

# Alternative: Use Gmail with App Password
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use your Gmail address as SMTP_USER
# 4. Use the generated App Password as SMTP_PASS

# For testing without real emails, you can use Ethereal Email:
# Visit https://ethereal.email/ to create a test account

# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual email credentials
# 3. For Gmail, use App Password instead of regular password
# 4. Make sure .env.local is added to .gitignore